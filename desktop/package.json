{
  "name": "modular-toolbox-tpl-desktop",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/dev.ts",
    "build:app": "mkdir -p build/ModularToolboxTpl.app/Contents/MacOS && bun build --compile --minify --sourcemap --target bun --external webview-bun --external sqlite3 --external reflect-metadata src/main.ts --outfile build/ModularToolboxTpl.app/Contents/MacOS/modular-toolbox-tpl.bin",
    "build:macos": "bun run scripts/build-macos.ts",
    "build:windows": "bun run scripts/build-windows.ts",
    "build:dispatch": "bun run scripts/build-dispatch.ts"
  },
  "dependencies": {
    "webview-bun": "^2.4.0",
    "@app/shared": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.4.0"
  }
}
