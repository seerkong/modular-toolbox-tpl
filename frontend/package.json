{
  "name": "modular-toolbox-tpl-frontend",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "../shared"
  ],
  "scripts": {
    "sync-static": "bun run ./scripts/sync-static.ts",
    "dev:vue": "bun run sync-static && cd packages/vue && bun run dev",
    "dev:react": "bun run sync-static && cd packages/react && bun run dev",
    "build:vue": "bun run sync-static && cd packages/vue && bun run build",
    "build:react": "bun run sync-static && cd packages/react && bun run build",
    "test": "cd packages/core && bun run test && cd ../mediator && bun run test"
  }
}
