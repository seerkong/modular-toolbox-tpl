import React from "react";

export const NestedLevel3: React.FC = () => (
  <div className="card">
    <div className="section-title">三级嵌套层级示例</div>
    <p style={{ margin: 0 }}>用于展示多层级菜单的三级页面占位。</p>
  </div>
);

export default NestedLevel3;
